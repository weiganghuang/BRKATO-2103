plugin: amazon.aws.aws_ec2
profile: msi
regions:
  - us-west-1
filters:
  tag:Name:
    - CL*

keyed_groups:
  - key: tags
    prefix: tag
  # - prefix: instance_type
  #   key: instance_type
  # - key: placement.region
  #   prefix: aws_region
    
